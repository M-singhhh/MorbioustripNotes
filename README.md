# MorbioustripNotes
imp linux commands : 
touch - cretes empty file "touch myfile.txt" 
echo - fills it with text  echo "This is my text" > myfile.txt
cat - opens a window where you can write multiple lines of file "cat > myfile.txt"
